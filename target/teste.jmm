program exemplo {
    final int x = 10;
    class C {
        int y[5];
    }
    void main() {
        x = x + 1;
        if (x >= 10) print(x);
        else read(x);
        while (x != 0) x = x - 1;
        return;
    }
}
